bplots(4) #2 rows

# x1=data.index
# y1=data["Percent Frames with Landmark"]

# mask = data["Average of Each Participant"].notnull()
# x2 = x1[mask]
# y2=data.loc[mask, "Average of Each Participant"]

# axs[0].bar(x1, y1)
# axs[0].set_title('Percent Frames w Landmarks- All Videos')

# axs[1].bar(x2, y2)
# axs[1].set_title('Percent Frames w Landmarks- Per Participant')

# plt.show()
