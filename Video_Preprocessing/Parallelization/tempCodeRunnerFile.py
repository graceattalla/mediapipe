t_folder:
    #         if os.path.isdir(os.path.join(folder,inner)): #check that it is a folder
    #             files = os.listdir(os.path.join(folder,inner))
    #             full_file_path = os.path.join(inner, file)
    #             for file in files:
    #                 if ".mp4" in file:
    #                     full_file_path = os.path.join(folder, inner, file)
    #                     print(f"----{full_file_path}")
    #                     